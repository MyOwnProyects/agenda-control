<?php 
use App\Library\FuncionesGlobales;
?>
<style>
    /* ============================================================
    SCOPE: Todo el CSS está encapsulado bajo .modal_cancelar_cita
    para no afectar otros modales del sistema.
    ============================================================ */

    /* Variables del sistema de diseño */
    .modal_cancelar_cita {
    --mcc-primary:              #4648d4;
    --mcc-primary-container:    #6063ee;
    --mcc-primary-fixed:        #e1e0ff;
    --mcc-on-primary:           #ffffff;
    --mcc-on-primary-fixed:     #07006c;
    --mcc-surface:              #f8f9ff;
    --mcc-surface-low:          #eff4ff;
    --mcc-surface-lowest:       #ffffff;
    --mcc-surface-variant:      #d9e3f6;
    --mcc-on-surface:           #121c2a;
    --mcc-on-surface-variant:   #464554;
    --mcc-outline-variant:      #c7c4d7;
    --mcc-tertiary-fixed:       #fff3cd;
    --mcc-on-tertiary-fixed:    #261a00;
    --mcc-on-tertiary-variant:  #5c4300;
    --mcc-tertiary:             #a07000;
    --mcc-error:                #ba1a1a;
    --mcc-error-bg:             #ffdad6;
    --mcc-radius-card:          12px;
    --mcc-radius-field:         10px;
    }

    /* Modal dialog */
    .modal_cancelar_cita .modal-dialog {
    max-width: 820px;
    }

    /* Modal content */
    .modal_cancelar_cita .modal-content {
    border: none;
    border-radius: 16px;
    overflow: hidden;
    box-shadow: 0 24px 48px -12px rgba(18,28,42,0.22);
    }

    /* ── Header ── */
    .modal_cancelar_cita .modal-header {
    border-bottom: 1px solid rgba(0,0,0,0.07);
    padding: 1.25rem 1.75rem 1rem;
    align-items: flex-start;
    }

    .modal_cancelar_cita .modal-header .modal-title {
    font-size: 1.2rem;
    font-weight: 800;
    color: var(--mcc-on-surface);
    line-height: 1.2;
    display: flex;
    flex-direction: column;
    gap: 5px;
    }

    /* Badge "ACTIVA" dentro del título */
    .modal_cancelar_cita .mcc-status-row {
    display: flex;
    align-items: center;
    gap: 8px;
    }
    .modal_cancelar_cita .mcc-status-label {
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--mcc-on-surface-variant);
    }
    .modal_cancelar_cita .mcc-status-badge {
    background: var(--mcc-primary-fixed);
    color: var(--mcc-primary);
    font-size: 10px;
    font-weight: 700;
    border-radius: 50px;
    padding: 1px 9px;
    }

    .modal_cancelar_cita .btn-close {
    filter: none;
    opacity: 0.5;
    transition: opacity 0.15s;
    }
    .modal_cancelar_cita .btn-close:hover {
    opacity: 1;
    }

    /* ── Body ── */
    .modal_cancelar_cita .modal-body {
    padding: 1.25rem 1.75rem 1.5rem;
    }

    /* Labels de sección */
    .modal_cancelar_cita .col-form-label,
    .modal_cancelar_cita .mcc-section-label {
    font-size: 10px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--mcc-on-surface-variant);
    margin-bottom: 6px;
    display: block;
    }

    .modal_cancelar_cita textarea.form-control {
    height: auto;
    padding-top: 10px;
    padding-bottom: 10px;
    resize: none;
    }

    /* ── Encabezado de sección numerada ── */
    .modal_cancelar_cita .mcc-section-heading {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 10px;
    margin-top: 18px;
    }
    .modal_cancelar_cita .mcc-section-num {
    width: 22px;
    height: 22px;
    border-radius: 50%;
    background: rgba(70,72,212,0.1);
    color: var(--mcc-primary);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-size: 10px;
    font-weight: 700;
    flex-shrink: 0;
    }
    .modal_cancelar_cita .mcc-section-title {
    font-size: 13px;
    font-weight: 700;
    color: var(--mcc-on-surface);
    margin: 0;
    }

    /* ── Radio cards (Tipo de Movimiento) ── */
    .modal_cancelar_cita .mcc-radio-card {
    background: var(--mcc-surface-low);
    border-radius: var(--mcc-radius-card);
    border: 1.5px solid transparent;
    padding: 0.9rem 1rem;
    cursor: pointer;
    height: 100%;
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 10px;
    transition: border-color 0.15s, background 0.15s;
    margin: 0;
    }
    .modal_cancelar_cita .mcc-radio-card.mcc-active {
    background: var(--mcc-primary-fixed);
    border-color: rgba(70,72,212,0.25);
    }
    .modal_cancelar_cita .mcc-radio-card-body {
    display: flex;
    flex-direction: column;
    gap: 3px;
    flex: 1;
    }
    .modal_cancelar_cita .mcc-radio-card-title {
    font-weight: 700;
    color: var(--mcc-on-surface);
    font-size: 13px;
    }
    .modal_cancelar_cita .mcc-radio-card-desc {
    font-size: 11.5px;
    color: var(--mcc-on-surface-variant);
    line-height: 1.5;
    }

    /* ── Radio pills (Citas Simultáneas + Gestión de Pagos) ── */
    .modal_cancelar_cita .mcc-radio-pill {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 10px;
    padding: 0.8rem 1rem;
    background: var(--mcc-surface-low);
    border-radius: var(--mcc-radius-card);
    border: 1.5px solid transparent;
    cursor: pointer;
    transition: background 0.15s, border-color 0.15s;
    margin: 0;
    width: 100%;
    }
    .modal_cancelar_cita .mcc-radio-pill.mcc-active {
    background: var(--mcc-primary-fixed);
    border-color: rgba(70,72,212,0.2);
    }
    .modal_cancelar_cita .mcc-pill-title {
    font-weight: 700;
    font-size: 13px;
    color: var(--mcc-on-surface);
    display: block;
    margin-bottom: 3px;
    }
    .modal_cancelar_cita .mcc-pill-desc {
    font-size: 11.5px;
    color: var(--mcc-on-surface-variant);
    line-height: 1.5;
    }

    /* ── Radio nativo — siempre a la derecha, color primary ── */
    .modal_cancelar_cita .mcc-radio-native {
    flex-shrink: 0;
    margin-top: 2px;
    width: 16px;
    height: 16px;
    accent-color: var(--mcc-primary);
    cursor: pointer;
    }

    /* ── Alertas informativas ── */
    .modal_cancelar_cita .mcc-alert {
    background: var(--mcc-tertiary-fixed);
    border-radius: var(--mcc-radius-field);
    padding: 0.75rem 1rem;
    display: flex;
    gap: 10px;
    align-items: flex-start;
    border: none;
    margin-top: 10px;
    }
    .modal_cancelar_cita .mcc-alert i,
    .modal_cancelar_cita .mcc-alert .bi {
    color: var(--mcc-tertiary);
    font-size: 18px;
    margin-top: 1px;
    flex-shrink: 0;
    }
    .modal_cancelar_cita .mcc-alert-body {
    font-size: 12.5px;
    color: var(--mcc-on-tertiary-variant);
    line-height: 1.5;
    }
    .modal_cancelar_cita .mcc-alert-body p { margin: 0 0 4px; }
    .modal_cancelar_cita .mcc-alert-body ul { margin: 0 0 4px; padding-left: 1.1rem; }
    .modal_cancelar_cita .mcc-alert-body li { margin-bottom: 2px; }
    .modal_cancelar_cita .mcc-alert-body .text-muted { font-size: 11px; color: var(--mcc-on-tertiary-variant) !important; opacity: 0.75; }
    .modal_cancelar_cita .mcc-alert-body strong { color: var(--mcc-on-tertiary-fixed); }

    /* ── Alerta error ── */
    .modal_cancelar_cita #alert_error {
    border-radius: var(--mcc-radius-field);
    background: var(--mcc-error-bg);
    border: none;
    color: var(--mcc-error);
    font-size: 13px;
    display: flex;
    align-items: center;
    gap: 8px;
    margin-top: 12px;
    }

    /* ── invalid-feedback ── */
    .modal_cancelar_cita .invalid-feedback {
    font-size: 11.5px;
    margin-top: 4px;
    }

    /* Utilidad hide */
    .modal_cancelar_cita .hide { display: none !important; }

    .modal_cancelar_cita .mcc-invalid {
        border-color: #dc3545 !important;
        background-color: #fff5f5 !important;
    }
</style>
<div class="container mt-4">
    <!-- Section 1: Search Filters -->
    <div>
        <h2><?php echo FuncionesGlobales::UpperString($translations['html_agenda_lock']); ?></h2>
    </div>
    <div id="header_filters" class="card mb-4">
        <div class="card-header">
            <h5><?php echo $translations['html_header_search']; ?></h5>
        </div>
        <div class="card-body">
            <form id="searchForm" class="row g-3">
                <div class="col-md-3">
                    <label for="input_rango_fecha_inicio" class="form-label"><?php echo $translations['html_range_date']; ?></label>
                    <div class="input-group">
                        <input type="date" class="form-control" id="input_rango_fecha_inicio" name="input_rango_fecha_inicio" placeholder="<?php echo $translations['html_range_date']; ?>">
                        <button class="btn btn-outline-secondary" type="button" id="btn_clear_fecha_inicio">
                            <i class="bi bi-x-lg"></i>
                        </button>
                    </div>
                </div>

                <div class="col-md-3">
                    <label for="input_rango_fecha_termino" class="form-label"><?php echo $translations['html_limit_date']; ?></label>
                    <div class="input-group">
                        <input type="date" class="form-control" id="input_rango_fecha_termino" name="input_rango_fecha_termino" placeholder="<?php echo $translations['html_limit_date']; ?>">
                        <button class="btn btn-outline-secondary" type="button" id="btn_clear_fecha_termino">
                            <i class="bi bi-x-lg"></i>
                        </button>
                    </div>
                </div>

                <div class="col-md-3">
                    <label for="select_profesionales" class="form-label"><?php echo $translations['html_title_professionals']; ?></label>
                    <select id="select_profesionales" class="select_profesionales">
                        <option value=""></option>
                        <?php foreach($all_professionals as $profesional): ?>
                            <option value="<?php echo $profesional['id']; ?>"><?php echo $profesional['nombre_completo']; ?></option>
                        <?php endforeach; ?>
                    </select>
                </div>

                <div class="col-md-12 text-end">
                    <button type="button" class="btn btn-outline-dark" id="btnBuscar">
                        <i class="bi bi-search"></i> 
                        <?php echo $translations['html_btn_search']; ?>
                    </button>
                    <?php if ($create): ?>
                        <button type="button" class="btn btn-outline-dark" id="btnCrearDiaInhabil">
                            <i class="bi bi-plus-lg"></i> 
                            <?php echo $translations['html_create_off_day']; ?>
                        </button>
                        <button type="button" class="btn btn-outline-dark" id="btnCrearDiaInhabilProfesional">
                            <i class="bi bi-plus-lg"></i> 
                            <?php echo $translations['html_create_professional_off_day']; ?>
                        </button>
                    <?php endif; ?>
                </div> 
            </form>
        </div>
    </div>

    <!-- Section 2: Data Table -->
    <div class="card">
        <div class="card-header">
            <h5><?php echo $translations['html_header_results']; ?></h5>
        </div>
        <div class="card-body">
            <table id="table_results" class="display table table-striped table-bordered" style="width:100%">
                <thead>
                    <tr>
                        <th><?php echo $translations['html_date']; ?></th>
                        <th><?php echo $translations['html_description'] .' / '.$translations['html_reason']; ?></th>
                        <th><?php echo $translations['html_type_lock']; ?></th>
                        <th><?php echo $translations['html_location']; ?></th>
                        <th><?php echo $translations['html_title_professional']; ?></th>
                        <th><?php echo $translations['html_action']; ?></th>
                    </tr>
                </thead>
            </table>
        </div>
    </div>
</div>

<!-- ============================================================
     MODAL HTML — estructura Bootstrap 5 original conservada
     ============================================================ -->
<div class="modal fade modal_cancelar_cita" id="modal_create_dia_inhabil" tabindex="-1" aria-labelledby="label_modal_cancelar_cita" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">

      <!-- Header -->
      <div class="modal-header">
        <h5 class="modal-title" id="label_modal_cancelar_cita">
          Modificar Estatus de Cita
        </h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>

      <!-- Body -->
      <div class="modal-body col-md-12">
        <form id="form_modal_create" class="needs-validation" novalidate>
          <div class="row g-3 mb-1">
            <div class="col-md-6 fix-padding">
                <label for="select_locaciones" class="form-label"><?php echo $translations['html_locations']; ?></label>
                <?php $count_locaciones = count($arr_locaciones); ?>
                <select id="select_locaciones" class="form-select" aria-label="Default select example" <?php echo $count_locaciones == 1 ? 'disabled' : ''; ?>>
                    <option></option>    
                    <?php foreach($arr_locaciones as $locacion): ?>
                        <option value="<?php echo $locacion['id']; ?>" <?php echo $count_locaciones == 1 ? 'selected' : ''; ?>><?php echo $locacion['nombre']; ?></option>
                    <?php endforeach; ?>
                </select>
                <div class="invalid-feedback"><?php echo $translations['html_error_selected_location']; ?></div>
            </div> 

            <div class="col-md-6"></div>            
                <!-- Campo Clave -->
                <div class="col-md-6">
                    <label for="input_fecha_inicio" class="col-form-label"><?php echo $translations['html_starting_date']; ?></label>
                    <input type="date" id="input_fecha_inicio" name="input_fecha_inicio" class="form-control" maxLength="10" required />
                    <div class="invalid-feedback"><?php echo $translations['html_error_date']; ?></div>
                </div>
                <div class="col-md-6">
                    <label for="input_fecha_termino" class="col-form-label"><?php echo $translations['html_ending_date']; ?></label>
                    <input type="date" id="input_fecha_termino" name="input_fecha_termino" class="form-control" maxLength="10" required />
                    <div class="invalid-feedback"><?php echo $translations['html_error_date']; ?></div>
                </div>
                <!-- Campo Nombre -->
                <div class="col-md-12">
                    <label for="input_label_bloqueo" class="col-form-label"><?php echo $translations['html_description']; ?></label>
                    <input type="text" class="form-control" id="input_label_bloqueo" required>
                    <div class="invalid-feedback"><?php echo $translations['html_error_description']; ?></div>
                </div>

          </div>

          <!-- 01 Tipo de Movimiento -->
          <div class="mcc-section-heading">
            <span class="mcc-section-num">
                <i class="bi-arrow-left-right"></i>
            </span>
            <span class="mcc-section-title">Tipo de Movimiento</span>
          </div>

          <div class="div_header row g-3">
            <div class="col-6">
              <div class="mcc-radio-card" data-group="estado_cita" data-value="pendiente">
                <div class="mcc-radio-card-body">
                  <span class="mcc-radio-card-title">Pendiente a reagendar</span>
                  <span class="mcc-radio-card-desc">Mantiene la cita en el sistema pero remueve el horario asignado.</span>
                </div>
                <input type="radio" class="mcc-radio-native tipo_movimiento" name="estado_cita" value="pendiente" required/>
              </div>
            </div>
            <div class="col-6">
              <div class="mcc-radio-card" data-group="estado_cita" data-value="cancelar">
                <div class="mcc-radio-card-body">
                  <span class="mcc-radio-card-title">Cancelar cita</span>
                  <span class="mcc-radio-card-desc">Elimina la reserva del calendario permanentemente.</span>
                </div>
                <input type="radio" class="mcc-radio-native tipo_movimiento" name="estado_cita" value="cancelar"/>
              </div>
            </div>
          </div>
          <div class="invalid-feedback d-block" id="error_estado_cita" style="display:none!important;"></div>

          <!-- Alertas tipo movimiento -->
          <div class="div_tipo_movimiento" style="display:none;">
            <div class="mcc-alert">
              <i class="bi bi-info-circle-fill"></i>
              <div class="mcc-alert-body">
                <div id="warning_pendiente" class="warning_message" style="display:none;">
                  <p class="fw-semibold">La cita dejará de aparecer en la agenda pero no está cancelada:</p>
                  <ul>
                    <li>El espacio quedará disponible para una nueva cita</li>
                    <li>Se marcará como FALTA.</li>
                  </ul>
                </div>
                <div id="warning_cancelar" class="warning_message" style="display:none;">
                  <p class="fw-semibold">La cita pasará a estar como cancelada y el espacio quedará disponible para una nueva cita.</p>
                  <p class="text-muted">Esta acción no se puede deshacer una vez confirmada.</p>
                </div>
              </div>
            </div>
          </div>

          <!-- 03 Gestión de Pagos -->
          <div class="div_header div_accion_pago">
            <div class="mcc-section-heading">
              <span class="mcc-section-num">
                <i class="bi-cash-stack"></i>
              </span>
              <span class="mcc-section-title">Acción sobre pagos aplicados</span>
            </div>
            <div class="row g-3">
              <div class="col-6">
                <div class="mcc-radio-pill" data-group="movto_pago" data-value="reasignar">
                  <div>
                    <span class="mcc-pill-title">Reasignar pagos</span>
                    <span class="mcc-pill-desc">Transfiere el monto abonado a la cita deudora más antigua del paciente (Esto incluye pagos pendientes por aplicar de esta y otras citas).</span>
                  </div>
                  <input type="radio" class="mcc-radio-native tipo_movto" name="movto_pago" value="reasignar" required/>
                </div>
              </div>
              <div class="col-6">
                <div class="mcc-radio-pill" data-group="movto_pago" data-value="saldo_favor">
                  <div>
                    <span class="mcc-pill-title">Dejar como saldo a favor</span>
                    <span class="mcc-pill-desc">El importe se acreditará como saldo positivo en la cuenta del paciente.</span>
                  </div>
                  <input type="radio" class="mcc-radio-native tipo_movto" name="movto_pago" value="saldo_favor"/>
                </div>
              </div>
            </div>
          </div>

          <!-- Alertas gestión de pagos -->
          <div class="div_tipo_movto" style="display:none;">
            <div class="mcc-alert mt-2">
              <i class="bi bi-info-circle-fill"></i>
              <div class="mcc-alert-body">
                <div id="warning_reasignar" class="warning_message" style="display:none;">
                  <p class="fw-semibold">El dinero asignado a esta cita se reasignará a la cita deudora más antigua del paciente.</p>
                  <ul><li>En caso de no haber citas deudoras, el dinero quedará como saldo a favor.</li></ul>
                  <p class="text-muted">Esta acción no se puede deshacer una vez confirmada.</p>
                </div>
                <div id="warning_saldo_favor" class="warning_message" style="display:none;">
                  <p class="fw-semibold">El dinero asignado a esta cita pasará a ser saldo a favor.</p>
                  <p class="text-muted">Esta acción no se puede deshacer una vez confirmada.</p>
                </div>
              </div>
            </div>
          </div>
        </form>
      </div>

      <!-- Footer -->
      <div class="modal-footer">
        <button type="button" class="btn btn-outline-dark" data-bs-dismiss="modal"><?php echo $translations['html_btn_cancel']; ?></button>
        <button type="button" id="btnSave" class="btn btn-primary"><?php echo $translations['html_btn_save']; ?></button>
      </div>

    </div>
  </div>
</div>
<!--    modal_create_dia_inhabil_profesional-->
<!--    MODAL PARA CREAR BLOQUEO A DOCENTE  -->
<div class="modal fade modal_cancelar_cita" id="modal_create_dia_inhabil_profesional" tabindex="-1" aria-labelledby="label_modal_cancelar_cita" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">

      <!-- Header -->
      <div class="modal-header">
        <h5 class="modal-title" id="label_modal_cancelar_cita">
          Modificar Estatus de Cita
        </h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>

      <!-- Body -->
      <div class="modal-body col-md-12">
        <form id="form_modal_create" class="needs-validation" novalidate>
          <div class="row g-3 mb-1">
            <div class="col-md-6 fix-padding div_select_profesionales" style="position: relative;">
                <label for="select_profesionales_modal" class="form-label"><?php echo $translations['html_title_professionals']; ?></label>
                <select id="select_profesionales_modal" class="form-select" aria-label="Default select example" required>
                    <option></option>    
                    <?php foreach($all_professionals as $profesional): ?>
                        <option value="<?php echo $profesional['id']; ?>"><?php echo $profesional['nombre_completo']; ?></option>
                    <?php endforeach; ?>
                    </select>
                <div class="invalid-feedback"><?php echo $translations['html_error_professional']; ?></div>
            </div>    
            <div class="col-md-6 fix-padding div_select_motivo" style="position: relative;">
                <label for="select_motivos_bloqueo" class="form-label"><?php echo $translations['html_type_lock']; ?></label>
                <select id="select_motivos_bloqueo" class="form-select" aria-label="Default select example" required>
                    <option></option>    
                    <?php foreach($motivos_bloqueo_agenda as $motivo): ?>
                        <option value="<?php echo $motivo['id']; ?>"><?php echo $motivo['nombre']; ?></option>
                    <?php endforeach; ?>
                    </select>
                <div class="invalid-feedback"><?php echo $translations['html_error_type_lock']; ?></div>
            </div>           
            <!-- Campo Clave -->
            <div class="col-md-6">
                <label for="input_fecha_inicio" class="col-form-label"><?php echo $translations['html_starting_date']; ?></label>
                <input type="date" id="input_fecha_inicio" name="input_fecha_inicio" class="form-control" maxLength="10" required />
                <div class="invalid-feedback"><?php echo $translations['html_error_starting_date']; ?></div>
            </div>
            <!-- Campo Nombre -->
            <div class="col-md-6">
                <label for="input_fecha_termino" class="col-form-label"><?php echo $translations['html_ending_date']; ?></label>
                <input type="date" id="input_fecha_termino" name="input_fecha_termino" class="form-control" maxLength="10" required />
                <div class="invalid-feedback"><?php echo $translations['html_error_ending_date']; ?></div>
            </div>

            

          </div>

          <!-- 01 Tipo de Movimiento -->
          <div class="mcc-section-heading">
            <span class="mcc-section-num">
                <i class="bi-arrow-left-right"></i>
            </span>
            <span class="mcc-section-title">Tipo de Movimiento</span>
          </div>

          <div class="div_header row g-3">
            <div class="col-6">
              <div class="mcc-radio-card" data-group="estado_cita" data-value="pendiente">
                <div class="mcc-radio-card-body">
                  <span class="mcc-radio-card-title">Pendiente a reagendar</span>
                  <span class="mcc-radio-card-desc">Mantiene la cita en el sistema pero remueve el horario asignado.</span>
                </div>
                <input type="radio" class="mcc-radio-native tipo_movimiento" name="estado_cita" value="pendiente" required/>
              </div>
            </div>
            <div class="col-6">
              <div class="mcc-radio-card" data-group="estado_cita" data-value="cancelar">
                <div class="mcc-radio-card-body">
                  <span class="mcc-radio-card-title">Cancelar cita</span>
                  <span class="mcc-radio-card-desc">Elimina la reserva del calendario permanentemente.</span>
                </div>
                <input type="radio" class="mcc-radio-native tipo_movimiento" name="estado_cita" value="cancelar"/>
              </div>
            </div>
          </div>
          <div class="invalid-feedback d-block" id="error_estado_cita" style="display:none!important;"></div>

          <!-- Alertas tipo movimiento -->
          <div class="div_tipo_movimiento" style="display:none;">
            <div class="mcc-alert">
              <i class="bi bi-info-circle-fill"></i>
              <div class="mcc-alert-body">
                <div id="warning_pendiente" class="warning_message" style="display:none;">
                  <p class="fw-semibold">La cita dejará de aparecer en la agenda pero no está cancelada:</p>
                  <ul>
                    <li>El espacio quedará disponible para una nueva cita</li>
                    <li>Se marcará como FALTA.</li>
                  </ul>
                </div>
                <div id="warning_cancelar" class="warning_message" style="display:none;">
                  <p class="fw-semibold">La cita pasará a estar como cancelada y el espacio quedará disponible para una nueva cita.</p>
                  <p class="text-muted">Esta acción no se puede deshacer una vez confirmada.</p>
                </div>
              </div>
            </div>
          </div>

          <!-- 03 Gestión de Pagos -->
          <div class="div_header div_accion_pago">
            <div class="mcc-section-heading">
              <span class="mcc-section-num">
                <i class="bi-cash-stack"></i>
              </span>
              <span class="mcc-section-title">Acción sobre pagos aplicados</span>
            </div>
            <div class="row g-3">
              <div class="col-6">
                <div class="mcc-radio-pill" data-group="movto_pago" data-value="reasignar">
                  <div>
                    <span class="mcc-pill-title">Reasignar pagos</span>
                    <span class="mcc-pill-desc">Transfiere el monto abonado a la cita deudora más antigua del paciente (Esto incluye pagos pendientes por aplicar de esta y otras citas).</span>
                  </div>
                  <input type="radio" class="mcc-radio-native tipo_movto" name="movto_pago" value="reasignar" required/>
                </div>
              </div>
              <div class="col-6">
                <div class="mcc-radio-pill" data-group="movto_pago" data-value="saldo_favor">
                  <div>
                    <span class="mcc-pill-title">Dejar como saldo a favor</span>
                    <span class="mcc-pill-desc">El importe se acreditará como saldo positivo en la cuenta del paciente.</span>
                  </div>
                  <input type="radio" class="mcc-radio-native tipo_movto" name="movto_pago" value="saldo_favor"/>
                </div>
              </div>
            </div>
          </div>

          <!-- Alertas gestión de pagos -->
          <div class="div_tipo_movto" style="display:none;">
            <div class="mcc-alert mt-2">
              <i class="bi bi-info-circle-fill"></i>
              <div class="mcc-alert-body">
                <div id="warning_reasignar" class="warning_message" style="display:none;">
                  <p class="fw-semibold">El dinero asignado a esta cita se reasignará a la cita deudora más antigua del paciente.</p>
                  <ul><li>En caso de no haber citas deudoras, el dinero quedará como saldo a favor.</li></ul>
                  <p class="text-muted">Esta acción no se puede deshacer una vez confirmada.</p>
                </div>
                <div id="warning_saldo_favor" class="warning_message" style="display:none;">
                  <p class="fw-semibold">El dinero asignado a esta cita pasará a ser saldo a favor.</p>
                  <p class="text-muted">Esta acción no se puede deshacer una vez confirmada.</p>
                </div>
              </div>
            </div>
          </div>
        </form>
      </div>

      <!-- Footer -->
      <div class="modal-footer">
        <button type="button" class="btn btn-outline-dark" data-bs-dismiss="modal"><?php echo $translations['html_btn_cancel']; ?></button>
        <button type="button" id="btnSave" class="btn btn-primary"><?php echo $translations['html_btn_save']; ?></button>
      </div>

    </div>
  </div>
</div>


<script>
    var module_bloqueo_agenda   = (function(){
        let draw            = 1;
        //  URL
        const controller    = '/Bloqueoagenda';
        const url_index     = controller+"/index";
        const url_create    = controller+"/create";
        const url_delete    = controller+"/delete";
        const url_update    = controller+"/update";

        //  PERMISOS
        const action_create = '<?php echo $create; ?>';
        const action_update = '<?php echo false; ?>';
        const action_delete = '<?php echo $delete; ?>';

        //  language
        const translations  = {
            emptyTable: "<?= $translations['table_no_data'] ?>",
            info: "<?= $translations['showing_info'] ?>",
            infoEmpty: "<?= $translations['no_records'] ?>",
            search: "<?= $translations['search'] ?>",
            paginate: {
                first: "<?= $translations['paginate_first'] ?>",
                last: "<?= $translations['paginate_last'] ?>",
                next: "<?= $translations['paginate_next'] ?>",
                previous: "<?= $translations['paginate_previous'] ?>"
            },
            title_update    : "<?= $translations['html_edit_record'] ?>",
            title_delete    : "<?= $translations['html_delete_record'] ?>",
            title_preview   : "<?= $translations['html_preview_record'] ?>",
            title_change    : "<?= $translations['html_active_deactivate'] ?>",
            confirm_delete  : "<?= $translations['html_confirm_delete']?>",
            deactivate_patient      : "<?= $translations['html_deactivate_patient']?>",
            activate_patient        : "<?= $translations['html_activate_patient']?>",
            schedule_appointments   : "<?= $translations['html_schedule_appointments']?>",
            monday: "<?php echo $translations['html_monday'];     ?>",
            thuesday: "<?php echo $translations['html_thuesday'];   ?>",
            wednesday: "<?php echo $translations['html_wednesday'];  ?>",
            thursday: "<?php echo $translations['html_thursday'];   ?>",
            friday: "<?php echo $translations['html_friday'];     ?>",
            saturday: "<?php echo $translations['html_saturday'];   ?>",     
            diagnosis_capture   :  "<?php echo $translations['html_diagnosis_capture'];   ?>",
            digital_record      :  "<?php echo $translations['html_digital_record'];   ?>",
        };

        //  LISTA DE ERRORES EN MODAL
        const error_list = {
            sin_permisos    : "<?php echo $translations['html_error_empty_list']; ?>",
            error_email     : "<?php echo $translations['html_error_email']; ?>",
            error_celular   : "<?php echo $translations['html_error_cellphone']; ?>",
            error_contrasena    : "<?php echo $translations['html_error_different_password']; ?>",
            error_length_contrasena : "<?php echo $translations['html_error_length_password']; ?>",
            error_spliced           : "<?php echo $translations['html_error_spliced']; ?>",
            error_birthdate         : "<?php echo $translations['html_error_birthdate']; ?>",
            error_range_date        : "<?php echo $translations['html_error_range_date']; ?>",
        }

        // Variable global para almacenar la instancia de DataTable
        let table;

        $(document).ready(function(){
            cargarTabla();

            selectToSelect2($("#header_filters"),"select_profesionales");

            $('#btn_clear_fecha_inicio').on('click', function() {
                $('#input_rango_fecha_inicio').val('');
            });

            $('#btn_clear_fecha_termino').on('click', function() {
                $('#input_rango_fecha_termino').val('');
            });

            // Evento click del botón "Buscar"
            $("#header_filters").on('click',"#btnBuscar",function() {
                cargarTabla();
            });

            //  EVENTO PARA CREAR DIA INHABIL
            $("#btnCrearDiaInhabil").on('click',function(){
                show_modal_dia_inhabil();
            });

            //  EVENTO PARA CREAR DIA INHABIL PARA UN PROFESIONAL
            $("#btnCrearDiaInhabilProfesional").on('click',function(){
                show_modal_dia_inhabil_profesional();
            });

            $("#table_results").on('click','.btn_edit',function(){
                let data_row    = $(this).closest('tr').data('rowData');
                if (data_row['id_profesional'] == null || data_row['id_profesional'] == ''){
                    show_modal_dia_inhabil(data_row);
                }

                if (data_row['id_profesional'] != null){
                    show_modal_dia_inhabil_profesional(data_row);
                }
            });

            $("#table_results").on('click','.btn_delete',async function(){
                const data_row  = $(this).closest('tr').data('rowData');
                console.log('data_row',data_row);
                
                const respuesta = await window.modalConfirm(translations['confirm_delete']);

                if (respuesta){
                    $.ajax({
                        url     : url_delete,
                        method  : 'post',
                        data    : {
                            id          : data_row['id'],
                            accion      : 'delete_dia_inhabil',
                            data_old    : data_row
                        },
                        success : function(data){
                            showAlert('success',data);
                            $("#btnBuscar").trigger('click');
                        },
                        error   : function(error){
                            console.log('error',error);
                            showAlert('danger',error.responseText);
                        }
                    })
                }
            });

            $("#table_results").on('click','.btn_preview',function(){
                let data_row    = $(this).closest('tr').data('rowData');
                if (data_row['id_profesional'] == null || data_row['id_profesional'] == ''){
                    show_modal_dia_inhabil($(this).closest('tr').data('rowData'),true);
                }

                if (data_row['id_profesional'] != null){
                    show_modal_dia_inhabil_profesional($(this).closest('tr').data('rowData'),true);
                }
            });

            //  EL ENTER EJECUTA EL BUSCAR SOLO SI NO HAY UN MODAL VISIBLE
            $(document).on('keydown', function(e) {
                if ($(document).find('.modal:visible').length == 0 && e.key === 'Enter') {
                    $("#btnBuscar").trigger('click');
                }
            });
        });

        // Función para inicializar o recargar la tabla
        function cargarTabla() {
            draw    ++;
            if (!table) {
                table = new DataTable('#table_results', {
                    dom: 'lrtip',
                    paging: true,
                    info: true,
                    lengthChange: false,
                    language: translations,
                    ordering: false,
                    serverSide: true,
                    ajax: {
                        url: url_index,
                        method: 'POST',
                        data: function (d) {
                            d.pageSize  = d.length == null || d.length == '' ? 1 : d.length;
                            d.offset    = d.start == null || d.start == '' ? 0 : d.start;
                            d.draw      = draw;
                            d.accion    = 'get_rows';
                            d.fecha_inicio      = $("#input_rango_fecha_inicio").val();
                            d.fecha_termino     = $("#input_rango_fecha_termino").val();
                            d.id_profesional    = $("#select_profesionales").val();
                        },
                        dataSrc: function (json) {
                            console.log("DataTables Response:", json); // <---- Depuración
                            return json.data;
                        }
                    },
                    columns: [
                        { data: 'label_fecha' }, // Columna 1
                        { data: 'label_bloqueo' }, // Columna 1
                        { data: 'label_tipo_bloqueo' }, // Columna 1
                        { data: 'nombre_locacion' }, // Columna 1
                        { data: 'nombre_profesional' }, // Columna 1
                        {
                            data: null, 
                            className: "text-center", // Centra el contenido de la celda
                            orderable: false, // Desactiva el ordenamiento para esta columna
                            render: function (data, type, row) {
                                draw    ++;
                                // `data` contiene el objeto completo de la fila
                                // Generamos un botón con un atributo dinámico para identificar la fila
                                let btn_preview =
                                '<button class="btn btn-outline-dark btn-sm btn_preview" data-id="'+data['id']+'" title="'+translations['title_preview'] +'">' +
                                    '<i class="bi bi-search"></i> '+
                                '</button>';
                                
                                let btn_update  = action_update ? 
                                    '<button class="btn btn-outline-dark btn-sm btn_edit" data-id="'+data['id']+'" title="'+translations['title_update'] +'">' +
                                        '<i class="bi bi-pencil-fill"></i>'+
                                    '</button>'
                                : '';

                                let btn_delete  = action_delete ? 
                                '<button class="btn btn-outline-dark btn-sm btn_delete" data-id="'+data['id']+'" data-clave="'+data['clave']+'" data-activo="'+data['activo']+'" title="'+translations['title_delete'] +'">' +
                                    '<i class="bi bi-trash3-fill"></i>'+
                                '</button>' : '';
                                return btn_preview +btn_update  +btn_delete;
                            }
                        }
                    ],
                    createdRow: function (row, data, dataIndex) {
                        // Guardar el objeto completo en la fila como data
                        $(row).data('rowData', data);
                    }
                });
            } else {
                draw    ++;
                table.ajax.reload();
            }
        }

        function show_modal_dia_inhabil(data_actual = null,preview = false){
            let modal_clone = $("#modal_create_dia_inhabil").clone().show();

            $(modal_clone).find('#select_locaciones').select2({
                placeholder     : 'Seleccione una opción',
                dropdownParent: $(modal_clone)
            });

            modal_clone.on('click', '.mcc-radio-card, .mcc-radio-pill', function () {
                $(this).find('.mcc-radio-native').prop('checked', true).trigger('change');
            });

            $(modal_clone).find('.modal-body').on('wheel', function() {
                $(modal_clone).find('#select_profesionales_modal, #select_motivos_bloqueo').select2('close');
            });

            $(modal_clone).find(".div_accion_pago").hide();

            // ── Tipo de Movimiento ──────────────────────────────────────
            modal_clone.on('change', '.tipo_movimiento', function () {
                const valor = $(this).val();
                modal_clone.find('.div_tipo_movimiento').show();
                modal_clone.find('.div_tipo_movimiento').find('.warning_message').hide();
                modal_clone.find('.div_tipo_movimiento').find('#warning_' + valor).show();

                //  SI LA ACCION ES CANCELAR CITA SE MUESTRA ACCION SOBRE PAGOS
                if (valor == 'cancelar'){
                    $(modal_clone).find('.div_accion_pago').show();
                } else {
                    $(modal_clone).find('.tipo_movto').prop('checked',false);//div_tipo_movto
                    $(modal_clone).find('.div_tipo_movto').hide();
                    $(modal_clone).find('.div_accion_pago').hide();
                }

                // Al seleccionar, remover el error visual del grupo
                $(this).closest('.div_header').find('.mcc-invalid').removeClass('mcc-invalid');
            });

            // ── Gestión de Pagos ────────────────────────────────────────
            modal_clone.on('change', '.tipo_movto', function () {
                const valor = $(this).val();
                modal_clone.find('.div_tipo_movto').show();
                modal_clone.find('.div_tipo_movto').find('#warning_reasignar, #warning_saldo_favor').hide();
                modal_clone.find('.div_tipo_movto').find('#warning_' + valor).show();

                $(this).closest('.div_header').find('.mcc-invalid').removeClass('mcc-invalid');
            });

            //$(modal_clone).find('.tipo_movimiento:first').trigger('click');

            //  SI DATA VIENE CON INFORMACION ES EDICION
            if (data_actual != null){
                data_actual['id_locacion'] = data_actual['id_locacion'] == null ? "-1" : data_actual['id_locacion'];
                $(modal_clone).find('#select_locaciones').val(data_actual['id_locacion']).trigger('change');
                $(modal_clone).find('#input_fecha_inicio').val(data_actual['fecha_inicio']);
                $(modal_clone).find('#input_fecha_termino').val(data_actual['fecha_termino']);
                $(modal_clone).find('#input_label_bloqueo').val(data_actual['label_bloqueo']);
            }

            $(modal_clone).on('click','#btnSave',async function(){
                let btn =  $(this);
                $(btn).prop('disable',true);

                const form = $(modal_clone).find('#form_modal_create')[0]; // Obtén el elemento DOM
                let flag_error  = false;
                if (form.checkValidity() === false) {
                    $(form).addClass('was-validated');
                

                    // Revisar cada campo que sea inválido
                    const invalidFields = Array.from(form.elements).filter(el => 
                        !el.validity.valid && $(el).is(':visible')
                    );

                    invalidFields.forEach(field => {
                        console.log('Campo inválido:', field,field.name || field.id, field.validationMessage);

                        if (field.type === 'radio') {
                            const groupName = field.name;
                            const isChecked = modal_clone.find(`input[name="${groupName}"]:checked`).length > 0;
                            if (!isChecked) {
                                // Marcar todas las cards/pills del grupo con borde rojo
                                modal_clone.find(`input[name="${groupName}"]`).closest('.mcc-radio-card, .mcc-radio-pill')
                                    .addClass('mcc-invalid');
                                // Mostrar el invalid-feedback del grupo
                                modal_clone.find(`input[name="${groupName}"]`).last()
                                    .closest('.row').next('.invalid-feedback')
                                    .addClass('d-block');
                            }
                        }
                    });

                    if (invalidFields.length > 0){
                        flag_error = true;
                    }
                } else {
                    $(form).removeClass('was-validated');
                }

                if (flag_error){
                    $(btn).prop('disabled',false);
                    return false;
                }

                let obj_info    = {
                    id_locacion     : $(modal_clone).find('#select_locaciones').val() == "-1" ? null : $(modal_clone).find('#select_locaciones').val(),
                    fecha_inicio    : $(modal_clone).find('#input_fecha_inicio').val(),
                    fecha_termino   : $(modal_clone).find('#input_fecha_termino').val(),
                    label_bloqueo   : $(modal_clone).find('#input_label_bloqueo').val(),
                    tipo_bloqueo    : '1',
                    tipo_movimiento : $(modal_clone).find('.tipo_movimiento:checked').val(),
                    tipo_accion_pagos   : $(modal_clone).find('input[name="movto_pago"]:checked').val()
                };

                if (data_actual != null){
                    obj_info['id']      = data_actual['id'];
                }

                //  SE VERIFICAN LA CANTIDAD DE CITAS ACTIVAS, ESTO PARA QUE EL USUARIO ESTE CONCIENTE
                //  SEL NUMERO DE CITAS AFECTADAS AL CREAR EL REGISTRO
                $.ajax({
                    url         : url_index,
                    method      : 'POST',
                    dataType    : 'json',
                    data        : {
                        obj_info    : {
                            id_locacion     : obj_info['id_locacion'],
                            tipo_busqueda   : 'activas',
                            fecha_inicio    : obj_info['fecha_inicio'],
                            fecha_termino   : obj_info['fecha_termino'],
                            from_catalog    : true,
                            tipo_accion_pagos   : $(modal_clone).find('input[name="movto_pago"]:checked').val()
                        },
                        accion      : 'verificar_citas'
                    },
                    success     : async function(num_citas_afectadas){
                        if (num_citas_afectadas == 0){
                            //  AJAX SAVE
                            $.ajax({
                                url         : data_actual != null ? url_update : url_create,
                                method      : 'POST',
                                dataType    : 'json',
                                data        : {
                                    obj_info    : obj_info,
                                    accion      : data_actual != null ? 'update_dia_inhabil' : 'create_dia_inhabil',
                                    data_old    : data_actual,
                                    num_citas_afectadas : num_citas_afectadas
                                },
                                success     : function(data){
                                    $(btn).prop('disabled',false);
                                    $(modal_clone).modal('hide');
                                    showAlert('success',data);
                                    cargarTabla();
                                },
                                error     : function(error){
                                    actionJsonError(error,btn);
                                }
                            })
                        } else {
                            let tipo_mensaje    = $(modal_clone).find('.tipo_movimiento:checked').val() == 'cancelar' ? 'CANCELADAS' : 'PENDIENTES POR AGENDAR';
                            const respuesta = await window.modalConfirm('Se marcarán en total <b>'+num_citas_afectadas+' cita(s)</b> como '+tipo_mensaje+'.<br>¿Deseas continuar?',true);

                            if (respuesta) {
                                $.ajax({
                                    url         : data_actual != null ? url_update : url_create,
                                    method      : 'POST',
                                    dataType    : 'json',
                                    data        : {
                                        obj_info    : obj_info,
                                        accion      : data_actual != null ? 'update_dia_inhabil' : 'create_dia_inhabil',
                                        data_old    : data_actual,
                                        num_citas_afectadas : num_citas_afectadas
                                    },
                                    success     : function(data){
                                        $(btn).prop('disabled',false);
                                        $(modal_clone).modal('hide');
                                        showAlert('success',data);
                                        cargarTabla();
                                    },
                                    error     : function(error){
                                        actionJsonError(error,btn);
                                    }
                                })
                            } else {
                                $(btn).prop('disabled',false);
                            }

                        }
                    },
                    error     : function(error){
                        actionJsonError(error,btn);
                    }
                })

                
            });

            if (preview){
                $(modal_clone).find('select').prop('disabled',true);
                $(modal_clone).find('input').prop('disabled',true);
                $(modal_clone).find('#btnSave').remove();
                $(modal_clone).find('#label_modal_create_dia_inhabil').text(translations['title_preview'])
                $(modal_clone).find('.div_nuevo_estatus').hide();
            }

            $(modal_clone).modal('show');
        }

        function show_modal_dia_inhabil_profesional(data_actual = null,preview = false){
            let modal_clone = $("#modal_create_dia_inhabil_profesional").clone().show();

            $(modal_clone).find('#select_profesionales_modal').select2({
                placeholder     : 'Seleccione una opción',
                dropdownParent  : $(modal_clone).find('.div_select_profesionales'),
            });

            $(modal_clone).find('#select_motivos_bloqueo').select2({
                placeholder     : 'Seleccione una opción',
                dropdownParent  : $(modal_clone).find('.div_select_motivo'),
            });

            $(modal_clone).find('.modal-body').on('wheel', function() {
                $(modal_clone).find('#select_profesionales_modal, #select_motivos_bloqueo').select2('close');
            });

            $(modal_clone).find(".div_accion_pago").hide();

            // ── Tipo de Movimiento ──────────────────────────────────────
            modal_clone.on('change', '.tipo_movimiento', function () {
                const valor = $(this).val();
                modal_clone.find('.div_tipo_movimiento').show();
                modal_clone.find('.div_tipo_movimiento').find('.warning_message').hide();
                modal_clone.find('.div_tipo_movimiento').find('#warning_' + valor).show();

                //  SI LA ACCION ES CANCELAR CITA SE MUESTRA ACCION SOBRE PAGOS
                if (valor == 'cancelar'){
                    $(modal_clone).find('.div_accion_pago').show();
                } else {
                    $(modal_clone).find('.tipo_movto').prop('checked',false);//div_tipo_movto
                    $(modal_clone).find('.div_tipo_movto').hide();
                    $(modal_clone).find('.div_accion_pago').hide();
                }

                // Al seleccionar, remover el error visual del grupo
                $(this).closest('.div_header').find('.mcc-invalid').removeClass('mcc-invalid');
            });

            // ── Gestión de Pagos ────────────────────────────────────────
            modal_clone.on('change', '.tipo_movto', function () {
                const valor = $(this).val();
                modal_clone.find('.div_tipo_movto').show();
                modal_clone.find('.div_tipo_movto').find('#warning_reasignar, #warning_saldo_favor').hide();
                modal_clone.find('.div_tipo_movto').find('#warning_' + valor).show();

                $(this).closest('.div_header').find('.mcc-invalid').removeClass('mcc-invalid');
            });

            //  SI DATA VIENE CON INFORMACION ES EDICION
            if (data_actual != null){
                $(modal_clone).find('#select_profesionales_modal').find('option').each(function(){
                    if ($(this).val() != data_actual['id_profesional']){
                        $(this).remove();
                    }
                });
                $(modal_clone).find('#select_profesionales_modal').val(data_actual['id_profesional']).trigger('change');
                $(modal_clone).find('#select_motivos_bloqueo').val(data_actual['id_motivo_cancelacion_cita']).trigger('change');
                $(modal_clone).find('#input_fecha_inicio').val(data_actual['fecha_inicio']);
                $(modal_clone).find('#input_fecha_termino').val(data_actual['fecha_termino']);
                $(modal_clone).find('#select_profesionales_modal').prop('disabled',true);
            }

            $(modal_clone).on('click','#btnSave',function(){
                let btn =  $(this);
                $(btn).prop('disable',true);

                const form = $(modal_clone).find('#form_modal_create')[0]; // Obtén el elemento DOM
                let flag_error  = false;
                if (form.checkValidity() === false) {
                    $(form).addClass('was-validated');
                    flag_error  = true;
                } else {
                    $(form).removeClass('was-validated');
                }

                const fecha_inicio  = moment($(modal_clone).find('#input_fecha_inicio').val());
                const fecha_termino = moment($(modal_clone).find('#input_fecha_termino').val());

                if (fecha_inicio.isAfter(fecha_termino)) {
                    $(modal_clone).find('#alert_error').find('#msg_error').html(error_list['error_range_date']);
                    $(modal_clone).find('#alert_error').addClass('d-flex').show();
                    flag_error  = true;
                } else {
                    $(modal_clone).find('#alert_error').removeClass('d-flex').hide();
                }

                if (flag_error){
                    return false;
                }

                let obj_info    = {
                    id_profesional      : $(modal_clone).find('#select_profesionales_modal').val(),
                    nombre_profesional  : $(modal_clone).find('#select_profesionales_modal').find('option:selected').text(),
                    fecha_inicio        : $(modal_clone).find('#input_fecha_inicio').val(),
                    fecha_termino       : $(modal_clone).find('#input_fecha_termino').val(),
                    id_motivo_cancelacion_cita  : $(modal_clone).find('#select_motivos_bloqueo').val(),
                    label_bloqueo               : $(modal_clone).find('#select_motivos_bloqueo').find('option:selected').text(),
                    tipo_bloqueo                : '2',
                    tipo_movimiento             : $(modal_clone).find('.tipo_movimiento:checked').val()
                };

                if (data_actual != null){
                    obj_info['id']      = data_actual['id'];
                }

                //  SE VERIFICAN LA CANTIDAD DE CITAS ACTIVAS, ESTO PARA QUE EL USUARIO ESTE CONCIENTE
                //  SEL NUMERO DE CITAS AFECTADAS AL CREAR EL REGISTRO
                $.ajax({
                    url         : url_index,
                    method      : 'POST',
                    dataType    : 'json',
                    data        : {
                        obj_info    : {
                            id_profesional  : $(modal_clone).find('#select_profesionales_modal').val(),
                            tipo_busqueda   : 'activas',
                            fecha_inicio    : obj_info['fecha_inicio'],
                            fecha_termino   : obj_info['fecha_termino'],
                            from_catalog    : true
                        },
                        accion      : 'verificar_citas'
                    },
                    success     : async function(num_citas_afectadas){
                        if (num_citas_afectadas == 0){
                            //  AJAX SAVE
                            $.ajax({
                                url         : data_actual != null ? url_update : url_create,
                                method      : 'POST',
                                dataType    : 'json',
                                data        : {
                                    obj_info    : obj_info,
                                    accion      : data_actual != null ? 'update_dia_inhabil_profesional' : 'create_dia_inhabil_profesional',
                                    data_old    : data_actual,
                                    num_citas_afectadas : num_citas_afectadas
                                },
                                success     : function(data){
                                    $(btn).prop('disabled',false);
                                    $(modal_clone).modal('hide');
                                    showAlert('success',data);
                                    cargarTabla();
                                },
                                error     : function(error){
                                    actionJsonError(error,btn);
                                }
                            })
                        } else {
                            let tipo_mensaje    = $(modal_clone).find('.tipo_movimiento:checked').val() == 'cancelar' ? 'CANCELADAS' : 'PENDIENTES POR AGENDAR';
                            const respuesta = await window.modalConfirm('Se marcarán en total <b>'+num_citas_afectadas+' cita(s)</b> como '+tipo_mensaje+'.<br>¿Deseas continuar?',true);

                            if (respuesta) {
                                //  AJAX SAVE
                                $.ajax({
                                    url         : data_actual != null ? url_update : url_create,
                                    method      : 'POST',
                                    dataType    : 'json',
                                    data        : {
                                        obj_info    : obj_info,
                                        accion      : data_actual != null ? 'update_dia_inhabil_profesional' : 'create_dia_inhabil_profesional',
                                        data_old    : data_actual,
                                        num_citas_afectadas : num_citas_afectadas
                                    },
                                    success     : function(data){
                                        $(btn).prop('disabled',false);
                                        $(modal_clone).modal('hide');
                                        showAlert('success',data);
                                        cargarTabla();
                                    },
                                    error     : function(error){
                                        actionJsonError(error,btn);
                                    }
                                })
                            } else {
                                $(btn).prop('disabled',false);
                            }

                        }
                    },
                    error     : function(error){
                        actionJsonError(error,btn);
                    }
                });
            });

            if (preview){
                $(modal_clone).find('select').prop('disabled',true);
                $(modal_clone).find('input').prop('disabled',true);
                $(modal_clone).find('#btnSave').remove();
                $(modal_clone).find('#label_modal_create_dia_inhabil_profesional').text(translations['title_preview'])
                $(modal_clone).find('.div_nuevo_estatus').hide();
            }

            $(modal_clone).modal('show');
        }
    })();
</script>